import React from 'react'
import ParentManager from '~/components/admins/parents/ParentManager'

const ParentManagerPage : React.FC = () => {
  return (
    <div>
      <ParentManager/>
    </div>
  )
}

export default ParentManagerPage
